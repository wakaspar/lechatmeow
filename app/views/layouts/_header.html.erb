<header>
  <div class="ui container">
    <div class="ui stackable">
      <div class="six wide column">
       <h1><%= link_to 'Le Chat Meow', root_path, class: "logo" %> <small>ALPHA</small> </h1>
      </div>

      <nav class="four wide column">


        <div class="ui secondary menu">
          <% if user_signed_in? %>

            <div class="ui right item">
              <%= link_to new_chatroom_path do %>
                <p> <i class="add circle icon"></i> Create New Chat</p>
              <% end %>
            </div>
            <div class="ui right item">
              <%= link_to new_cat_path do %>
                <p> <i class="paw icon"></i> Make A New Cat</p>
              <% end %>
            </div>

            <div class="ui right dropdown item">


              <span style="color:#1E90FF;">  <%= current_user.first_name %> </span>
              <i class="dropdown icon"></i>
              <div class="menu">
                <div class="item">
                  <%= link_to 'Profile', user_path(current_user.id) %>
                </div>
                <div class="item">
                  <%= link_to 'Settings', edit_user_registration_path %>
                </div>
                <div class="item">
                  <%= link_to 'Log out', destroy_user_session_path, method: :delete %>
                </div>
              </div>
            </div>
          <% else %>
            <%= link_to 'Log In', new_user_session_path, class: "item" %>
            <%= link_to 'Sign Up', new_user_registration_path, class: "item" %>
          <% end %>
        </div>
      </nav>
    </div>
  </div>
</header>
